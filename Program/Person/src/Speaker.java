public interface Speaker {
    /**
     * An interface is not allowed to declare full function body,
     * it can only declare name, parameter, and return-type of the function.
     *
     *
     * */
    void shout();
    void speak();
    int something();
}
